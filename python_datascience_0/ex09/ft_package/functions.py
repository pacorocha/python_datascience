def count_in_list(list_, item):
    """
    Counts the ocurrences of an item in a list.

    Parameters:
        list_ (list): The list to search in.
        item: The item to count occurrences of.

    Returns:
        int: The number of occurrences of the item in the list.
    """
    return list_.count(item)
